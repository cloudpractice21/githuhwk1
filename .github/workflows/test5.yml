name: Shell Command

on: [push]

jobs:
    run-sehll-command: 
      runs-on: ubuntu-latest
      steps:
        - name: echo a string
          run: eccho "hello world sudhakar"
        - name: multiline script
          run: |
            node -v
            npm -v


